Future<void> uploadFile(
  Map<String, Object?> authorization,
  bool video,
  void Function(double) progress,
) async {
  throw UnsupportedError('Use the CMS in a web browser to upload media.');
}

void openPreview(String url) {}
